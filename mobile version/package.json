{
  "name": "lcplanner-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor mobile wrapper for Your LC Meal Companion",
  "scripts": {
    "copy:web": "node ./scripts/copy-web-assets.mjs",
    "sync": "npm run copy:web && npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.1.0",
    "@capacitor/core": "^7.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.1.0"
  }
}